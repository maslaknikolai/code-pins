/** Page-level layout for the webview; pin nodes style themselves with Tailwind classes. */
export const panelStyles = /* css */ `
	html, body, #root {
		height: 100%;
		margin: 0;
		padding: 0;
		overflow: hidden;
	}
`;
